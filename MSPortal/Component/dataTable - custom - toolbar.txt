<div class="row mx-3 position-relative custom-loading">
  <div class="col-12 custom-datatable-toolbar">
    <div class="row">
      <div class="col-8">
        <select class="form-control form-control-toolbar form-control-sm" id="select-site">

          {%for site in list_site%}
          <option value="{{site.id}}">{{site.name}}</option>
          {% endfor %}

        </select>

        <select class="form-control form-control-toolbar form-control-sm ml-2" id="select-status">
          <option>-- Status --</option>
          <option value="activate">Activate</option>
          <option value="finished">Finished</option>
        </select>

        <select class="form-control form-control-toolbar form-control-sm ml-2" id="select-plan">
          <option>-- Plan --</option>
          <option value="plan_a">Plan A</option>
          <option value="plan_b">Plan B</option>
        </select>
      </div>

      <div class="col-4 text-right">
        <button data-toggle="modal" data-target="#modalCreatePartner" class="btn btn-light border btn-sm" type="button"
          margin="100px" data-toggle="modal" data-target="#modalCreatePartner" data-list-form="{{list_form}}"
          data-dict-form="{{dict_form}}" data-list-partner="{{list_partner_info}}" id="modal-create">Create</button>
      </div>
    </div>

  </div>

  <div class="col-12 mt-3">
    <table id="table_lp" class="table table-not-in-card table-wrapper table-hover">
      <thead>
        <tr>
          <th class="no-sort">#</th>
          <th class="no-sort">Logo</th>
          <th>Partner name</th>
          <th>Status</th>
          <th>Start date</th>
          <th>Contract period</th>
          <th>Plan</th>
          <th>Overview Report (PPTX)
            <i class="fa fa-info-circle ml-1 icon-tooltip-explain" data-toggle="tooltip"
              title="Overview Report (PPTX) in this month"></i>
          </th>
          <th>Contact Report (Excel)
            <i class="fa fa-info-circle ml-1 icon-tooltip-explain" data-toggle="tooltip"
              title="Contact Report (Excel) in this month"></i>
          </th>
          <th class="text-nowrap">Action</th>
        </tr>
      </thead>
      <tbody>
        {% for partner in customer_partner_info %}
        <tr>
          <td>{{loop.index}}</td>
          <td> <img src="{{partner.logo}}" alt="Logo" class="logo-in-table" data-toggle="popover"
              data-content="<img src={{partner.logo}} width='300px'/>" data-html='true' /></td>
          <td>{{partner.name}}</td>
          <td>
            <span class="badge badge-success">Activate</span>
          </td>
          <td>{{partner.created_date.strftime('%Y-%m-%d')}}</td>
          <td>6 months</td>
          <td>A Plan</td>
          <td>
            <span class="badge badge-success">Finished</span>

          </td>
          <td>
            <span class="badge badge-success">Finished</span>

          </td>
          <td class="text-nowrap">
            <a class="btn-light border btn-sm d-inline dropdown-in-table"
              href="{{url('partner_report_index',args=(partner.id,))}}">Report detail</a>

            <div class="dropdown d-inline">
              <button class="btn-light border dropdown-toggle btn-sm dropdown-in-table" type="button"
                id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                More
              </button>
              <div class="dropdown-menu " aria-labelledby="dropdownMenuButton">
                <a class="dropdown-item" href="{{url('partner_update',args=(partner.pk,))}}"><i
                    class="fa fa-edit mr-1"></i>Update</a>
                <a class="dropdown-item text-danger" href="{{url('partner_delete',args=(partner.pk,))}}"><i
                    class="fa fa-trash mr-1 text-danger"></i>Delete</a>
              </div>
            </div>
          </td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</div>